[workspace]
members = ["crates/*"]
resolver = "3"

[workspace.package]
# Single source of truth for the workspace's version: every `[package]` block below (and in
# `crates/*/Cargo.toml`) sets `version.workspace = true` instead of repeating this literal. It cannot
# fully eliminate the number from the manifests, though — see the comment on the
# `lci-codegraph-model` / `lci-codegraph-spring` entries in `[workspace.dependencies]` below for the
# one place it still has to appear as a real version requirement, and how that place is guarded.
version = "0.1.0"
edition = "2024"
# 1.88, not 1.85 (the edition-2024 floor): `lopdf` >= 0.42 uses let-chains, stabilised in 1.88.
# Note lopdf 0.42 itself still DECLARES `rust-version = "1.85"` — that declaration is wrong, and
# `cargo check` on 1.85/1.87 fails with E0658 in `lopdf/src/object.rs`. Verified empirically here:
# 1.87 fails, 1.88 passes. Do not lower this to match lopdf's own manifest.
rust-version = "1.88"
license = "MIT"
repository = "https://github.com/vymalo/lci-codegraph"
homepage = "https://github.com/vymalo/lci-codegraph"

# Dependencies more than one crate in the workspace needs, pinned once so every member resolves the
# same version. `tree-sitter`/`tree-sitter-java`/`tracing` are hoisted ahead of actually having a
# second consumer: a framework-extractor crate (Java/Spring-aware, parsing with the same grammar) is
# the next crate to land in `crates/`, and it will need exactly these three.
[workspace.dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
tracing = "0.1.31"
tree-sitter = "0.26"
tree-sitter-java = "0.23"
# The workspace's own internal crates, hoisted here (rather than declared per-consumer) so every
# consumer — currently the root package and `lci-codegraph-spring` — resolves the same path and
# version. Paths in `[workspace.dependencies]` are relative to the workspace root, not to the
# consuming crate's own directory, which is what lets one declaration here serve both.
#
# The `version = "0.1.0"` requirement on each line below is the one place the workspace version
# number still can't be collapsed to `version.workspace = true`: crates.io requires a real version
# requirement (not just a path) on a dependency once it's a published crate, so this literal has to
# stay in sync with `[workspace.package].version` above by hand. It is guarded, not just trusted:
# `.github/workflows/release.yml`'s `verify-version` job asserts, before any publish, that this
# requirement resolves to the same version as the release tag and every workspace member — see that
# job for the check that would fail if this line ever drifted.
lci-codegraph-model = { path = "crates/lci-codegraph-model", version = "0.1.0" }
lci-codegraph-spring = { path = "crates/lci-codegraph-spring", version = "0.1.0" }

[package]
name = "lci-codegraph"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/lci-codegraph"
description = "Code-graph extractor: semantic chunking, a cross-file structural call graph, and embedding-prep over one tree-sitter parse."
readme = "README.md"
keywords = ["tree-sitter", "code-graph", "static-analysis", "chunking", "indexing"]
categories = ["development-tools", "parser-implementations"]
exclude = ["/.github", "/docs", "/tests/fixtures/real-repos"]

[lib]
path = "src/lib.rs"

# This crate is a pure extractor: it parses a checkout and returns chunk + graph values. It never
# talks to a datastore, a cluster, or a forge — hosts map the output onto their own payloads and
# submit. Keep that boundary: no kube/sqlx/forge clients belong in `[dependencies]`.
#
# ONE deliberate exception: `ureq`, for the optional embeddings HTTP call (`src/embed/`). Embedding
# is the one step in this crate's job that is *inherently* a network round-trip to a model endpoint —
# there is no local/in-process model, so "extractor talks to nothing" and "the crate can embed" are
# incompatible, and we chose to keep embedding in-crate rather than push every host into re-implementing
# batching/truncation/retry against the graph-aware context header. It stays a narrow exception: still
# no kube/sqlx/forge client, and the embeddings call is the ONLY thing `ureq` is used for.
[dependencies]
anyhow = "1.0.103"
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
# Compile-time builders for the wide config constructors (`WalkOptions`, `IgnoreConfig`).
bon = "3.9.3"
# The shared node/edge vocabulary (and the framework-facts seam a framework-extractor crate targets) —
# see `crates/lci-codegraph-model` and `docs/architecture.md` for why this is a separate crate rather
# than a module.
lci-codegraph-model.workspace = true
# Spring-aware structural facts (routes, Feign boundaries, Spring Data repository methods), run as a
# sibling pass over the same parse `graph::extract_file` already has. A separate CRATE, not a module
# here, because `docs/design/spring-aware-graph.md` §5.2 draws a real distinction: a curated
# annotation/marker-interface allowlist churns every Spring release, which is a narrower and much
# noisier coupling than a tree-sitter grammar (changes rarely, maintained by someone else). Behind a
# crate boundary, "does the core know about Spring?" is answerable by reading this manifest's
# dependency list, not by auditing every module for a stray `use` — and it keeps that churn from ever
# leaking into `src/graph`, which stays entirely framework-agnostic.
lci-codegraph-spring.workspace = true
# Tree-sitter grammars. The structural graph is edges built on top of the symbols these find.
tree-sitter = { workspace = true }
tree-sitter-rust = "0.24"
tree-sitter-javascript = "0.25"
tree-sitter-typescript = "0.23.2"
tree-sitter-python = "0.25"
tree-sitter-java = { workspace = true }
# ripgrep's ignore engine: honours the repo's own `.gitignore` AND lets us layer an
# operator-configurable glob list on top with the SAME gitignore semantics (`GitignoreBuilder`).
# This is how the crate *composes with* — rather than replaces — the repo ignores.
ignore = "0.4"
# Pure-Rust PDF *text* extraction. Treated as untrusted-input code: bytes are capped BEFORE the
# parser sees them and the parse is wrapped in `catch_unwind` (it can panic on malformed input).
#
# Floor of 0.12 is a SECURITY floor, not a preference: it is the first release that depends on
# `lopdf >= 0.42`, which patches RUSTSEC-2026-0187 — unbounded recursion on deeply nested PDF
# objects that blows the stack and aborts the process with SIGABRT. That is an abort, not a panic,
# so this crate's `catch_unwind` guard around the parse does NOT contain it: a ~21 KB crafted file
# would take down the whole host process. Never relax this below 0.12.
pdf-extract = "0.12"
# Pre-flight, *bounded* inflate of a PDF's content streams to catch a decompression bomb BEFORE
# `pdf-extract` (which wraps `lopdf`/`flate2`) inflates them unbounded.
flate2 = "1"
# Blocking HTTP client for the OpenAI-compatible `/embeddings` call. Deliberately blocking, not async:
# `walk_checkout` is synchronous CPU work that hosts already wrap in `spawn_blocking` (see `src/walk.rs`
# doc comment), so a blocking client keeps that contract instead of forcing an async runtime onto every
# consumer just for this one optional call. Already resolved in `Cargo.lock` at 3.3.0 via the
# `container-tests` dev chain (rustls+ring defaults), so this adds no new TLS stack and no new licence
# surface for `cargo-deny`.
ureq = "3.3"

[dev-dependencies]
tempfile = "3.27"
# Used ONLY to synthesise valid fixture PDFs in tests (a proper xref table is fiddly to write by
# hand). `pdf-extract` already pulls this transitively. Never a runtime dependency.
# Kept in lockstep with the version `pdf-extract` resolves, so the fixture builder and the parser
# under test agree on the format — and so this dev-dep cannot drag the RUSTSEC-2026-0187 lopdf back
# into the lockfile behind the runtime floor above.
lopdf = "0.42"
# --- container-test suite (see the `container-tests` feature below) ---
# Dev-dependencies cannot be optional in Cargo, so these always resolve; the *tests* that use them
# are gated behind `required-features` so a plain `cargo test` needs no Docker daemon.
# Pinned to 0.27 deliberately: `testcontainers-modules` 0.15 depends on `testcontainers ^0.27`, and
# 0.28 pulls an incompatible `bollard-stubs`. Bump both together, never one alone.
testcontainers = "0.27"
testcontainers-modules = { version = "0.15", features = ["neo4j"] }
neo4rs = "0.8"
tokio = { version = "1.53", features = ["macros", "rt-multi-thread", "time", "process"] }

[features]
# Opt-in gate for the Docker-backed suites. `cargo test --features container-tests` requires a
# reachable Docker daemon; plain `cargo test` runs the unit + integration suites only.
container-tests = []

# The Docker-backed suites. Each is a separate target so they can be run (and timed) independently.
#
# `container_neo4j`  — loads the emitted nodes/edges into a real Neo4j with the same generic
#                      `:Symbol` + `[:REL {relation}]` write a host performs, then runs the
#                      `graph_find_symbol` / `graph_get_callers` retrieval queries against it.
# `container_build`  — builds and runs the crate inside Linux glibc and musl containers, proving the
#                      tree-sitter C grammars compile and link off the host toolchain.
# `container_repos`  — clones pinned real-world repositories inside a container and asserts the walk
#                      holds its invariants on input nobody wrote for the tests.
[[test]]
name = "container_neo4j"
required-features = ["container-tests"]

[[test]]
name = "container_build"
required-features = ["container-tests"]

[[test]]
name = "container_repos"
required-features = ["container-tests"]
